---
description: Conventions for the generator code
paths:
  - generator/**/*.py
---

- Python 3.13+, `uv` as package manager
- stdlib only (`urllib`, `re`, `pathlib`) — no third-party HTTP/parsing libraries
- No external runtime dependencies
