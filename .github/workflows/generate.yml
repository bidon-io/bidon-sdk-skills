name: Generate References
on:
  workflow_dispatch:
    inputs:
      skills:
        description: Skills to generate (comma-separated or "all")
        type: string
        default: all
      endpoint:
        description: Docs server URL
        type: string
        default: https://docs.bidon.org
jobs:
  generate:
    name: Fetch docs and build references
    runs-on: ubuntu-latest
    steps:
      - run: echo "TODO"
